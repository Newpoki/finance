/**
 * Must have a Page component at base of parallel route
 * otherwise, it route isn't matched anymore, it will keep the last match slot
 *
 * https://github.com/vercel/next.js/issues/64807
 */
export default function OverviewDialogPage() {
  return null
}
